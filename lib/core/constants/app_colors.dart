import 'package:flutter/material.dart';

class AppColors {
  // Pure Monochrome
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const gray = Color(0xFF888888);
  static const lightGray = Color(0xFFCCCCCC);
  static const darkGray = Color(0xFF444444);
  
  // Semantic Mappings
  static const background = black;
  static const surface = darkGray;
  static const surfaceLight = gray;
  
  // Board Colors
  static const boardLight = white;
  static const boardDark = black;
  
  // Text
  static const textPrimary = white;
  static const textSecondary = gray;
  
  // UI Elements
  static const cardBackground = Color(0xFF1A1A1A);
  static const divider = white;
}
